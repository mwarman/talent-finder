{
  "name": "@talent-finder/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "clean": "rimraf coverage dist",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "aws-cdk-lib": "2.257.0",
    "constructs": "10.6.0"
  }
}
