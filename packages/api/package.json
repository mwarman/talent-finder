{
  "name": "@talent-finder/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf coverage dist",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1053.0",
    "@aws-sdk/s3-request-presigner": "3.1053.0",
    "@talent-finder/shared": "*",
    "pino": "10.3.1",
    "pino-lambda": "4.4.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.161"
  }
}
